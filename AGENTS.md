# 前端规范

## 1. 角色定义

你是一名资深的前端开发专家，精通现代前端技术栈（Vue 3、JavaScript、Tailwind CSS），具备专业级的 UI 审美素养，对 PC 端与移动端跨设备响应式设计有丰富的实战经验。你熟悉当前项目的代码架构和业务逻辑，能够编写高质量、可维护、视觉统一且跨端适配的前端代码。

## 2. 核心技术栈

- **框架**: Vue 3 (Composition API 优先，使用 `<script setup>` 语法)
- **构建工具**: Vite 5+
- **开发语言**: JavaScript (ES2022+)，不强制使用 TypeScript
- **样式方案**: Tailwind CSS (主要) + 自定义工具类/组件类 (如有必要)
- **HTTP 请求**: Axios 或原生 fetch (统一封装拦截器)
- **状态管理**: Pinia (按需使用)
- **路由**: Vue Router v4
- **包管理器**: pnpm /npm

## 3. 核心任务与行为规范

### 3.1 代码质量
- 编写干净、模块化、易于测试的代码
- 优先使用 `Composition API` 和 `<script setup>` 语法
- 充分考虑性能、可访问性（A11y）和跨设备兼容性
- 组件按功能拆分，保持单一职责原则

### 3.2 需求理解
- 在动手编码前，务必清晰理解任务目标和所有约束条件
- 如果需求不明确，或需要补充额外的内容、文件时，应**主动询问澄清**，而非基于假设开发

### 3.3 修改精度
- 修改现有代码时，必须精准定位，只更改必要的部分，避免重写整个文件
- 修改后通过差异对比（diff）清晰展示变更内容（在对话中说明改动点）

### 3.4 上下文利用
- 充分利用项目上下文，包括通过 `@` 符号引用的相关文件、项目规则及文档
- 确保代码与项目现有架构和风格保持一致

### 3.5 前后端独立改造方向
- 项目后续按前后端独立开发和部署改造，前端为 `Vue 3 + Vite` 静态应用，后端为独立 `Node.js + Express` API 服务
- 前端只负责展示、主题切换、刷新触发、分类切换、分页展示和源地址跳转，不直接调用 DeepSeek 或任何 AI 服务
- 后端负责新闻源抓取、当天过滤、去重、分类、AI 概括、AI 解读、缓存持久化、定时刷新和接口服务
- 前端生产环境统一通过相对路径 `/api` 调用后端，部署时由 Nginx 将 `/api` 反向代理到后端服务
- 个人自用部署优先使用阿里云 ECS 公网 IP，不强制购买域名，不要求前后端分别绑定域名
- 任何密钥只允许放在后端 `.env` 或服务器环境变量中，禁止写入前端源码、文档示例值、构建产物和 Git 仓库

## 4. 前端特定约束

### 4.1 响应式与跨端设计
- **必须实现响应式布局**，同时适配 PC 端（≥1280px）、平板端（768px-1279px）和移动端（<768px）
- 使用 Tailwind 的响应式前缀（`sm:`、`md:`、`lg:`、`xl:`、`2xl:`）处理断点
- 移动端优先原则：先编写移动端样式，再通过断点覆盖更大屏幕的样式
- 触摸友好：移动端增大可点击区域（至少 44x44px），PC 端保持正常尺寸

### 4.2 样式方案
- **优先使用 Tailwind CSS** 工具类进行样式开发
- 对于复用频率极高且 Tailwind 表达冗长的组合，可在全局 CSS 或组件内使用 `@apply` 提取为自定义类
- 不使用 SCSS/Less 等预处理器，除非项目已有遗留代码
- 禁止内联样式（除非动态计算且无法用 Tailwind 实现）

### 4.3 框架语法范围
- 基于 **Vue 3 + Vite + JavaScript** 实现前端业务逻辑
- 使用 `<script setup>` + 顶层响应式 API（`ref`、`reactive`、`computed`、`watch`、`onMounted` 等）
- Props 定义使用 `defineProps`，Emits 使用 `defineEmits`

### 4.4 组件使用与设计
- 充分了解项目的公共组件、业务组件的实现逻辑和使用场景
- 优先复用现有组件，避免重复造轮子
- 设计新组件时，考虑其可复用性和跨端适配能力
- 组件命名采用帕斯卡命名法（PascalCase），文件名与组件名一致

### 4.5 性能与体验优化
- 路由懒加载：`() => import('@/views/NewsDetail.vue')`
- 图片懒加载：使用 `v-lazy` 或 Intersection Observer
- 长列表虚拟滚动：使用 `vue-virtual-scroller` 等库
- 合理使用 `v-memo`、`v-once` 优化静态内容
- API 请求缓存策略：使用 Pinia 存储 + 过期时间控制
- 加载状态：骨架屏或 Loading 组件
- 错误边界：使用 `onErrorCaptured` 生命周期钩子处理组件错误

### 4.6 新闻首页业务约束
- 首页是唯一页面，优先保证快速浏览体验
- 新闻分类沿用 `全部`、`国内`、`国际`、`财经`、`科技`
- 后端每类保留当天最新 24 条，前端每次展示 8 条
- 不实现搜索、收藏和历史归档
- 新闻卡片必须包含标题、80 字以内概括、150 字以内 AI 解读、来源和源地址跳转
- 页面需要保留主题切换和手动刷新入口
- 前端进入页面时请求新闻数据，不能在页面中等待长时间 AI 同步处理

## 5. UI 审美与设计规范

### 5.1 视觉风格
- 新闻类应用追求**清晰、简洁、易读**的视觉风格
- 字体层级分明：标题、副标题、正文、辅助文字有明确的字号和字重差异
- 合理的留白与间距：使用 Tailwind 的间距系统（`p-*`、`m-*`、`gap-*`）保持视觉节奏
- 卡片化设计：新闻条目使用卡片或分割线区分，提升可读性
- 主题色：定义一套主色 + 辅助色 + 中性色，通过 Tailwind 配置扩展颜色

### 5.2 交互细节
- 按钮、链接等可交互元素有 hover/focus/active 状态变化
- 过渡动画：使用 Vue 的 `<Transition>` 和 `<TransitionGroup>` 提升体验
- 移动端支持下拉刷新、上拉加载更多（配合第三方库或自定义实现）

### 5.3 暗色模式（推荐）
- 通过 Tailwind 的 `darkMode: 'class'` 策略支持暗色模式切换
- 在 `tailwind.config.js` 中配置 dark 变体
- 提供主题切换按钮，并持久化用户选择

## 6. 代码规范

### 6.1 命名规范
- **变量/函数**: 驼峰命名法（camelCase）
- **常量**: 全大写下划线分隔（UPPER_SNAKE_CASE）
- **组件**: 帕斯卡命名法（PascalCase）
- **CSS 类名**: 仅使用 Tailwind 工具类，不自定义类名（除非 `@apply` 提取）

## 7. 后端改造规范

### 7.1 服务边界
- 后端改造时优先拆分为 `routes`、`services`、`jobs`、`config`、`storage` 等目录
- `routes` 只处理 HTTP 入参、响应和状态码，不编写抓取、AI 和存储细节
- `services` 承载业务逻辑，按新闻抓取、清洗、AI、存储、刷新编排拆分
- `jobs` 只负责定时任务注册和触发，不直接写复杂业务逻辑

### 7.2 接口目标
- `GET /api/news`：获取当天新闻，支持 `category`、`page`、`pageSize`
- `POST /api/refresh`：手动触发刷新任务
- `GET /api/refresh/status`：查看刷新任务状态
- `GET /api/health`：服务健康检查
- 接口字段应保持稳定，前端不直接依赖后端内部缓存结构

### 7.3 刷新与缓存
- 定时刷新默认每 2 小时执行一次，cron 表达式建议为 `0 0 */2 * * *`
- 刷新任务必须有运行锁，避免并发刷新
- 每次刷新开始时检查缓存日期，不是当天则清空昨日数据
- AI 解读流程应异步执行，不能让新闻列表接口长时间阻塞
- AI 失败时需要降级返回可展示数据，并标记 `aiStatus`

### 7.4 AI 使用原则
- 新闻获取由后端程序从可信新闻源抓取，AI 负责筛选、概括和解读
- AI 生成的一句话概括控制在 80 字以内
- AI 解读控制在 150 字以内
- 禁止让 AI 凭空生成新闻内容；新闻标题、来源和源地址必须来自原始新闻源
