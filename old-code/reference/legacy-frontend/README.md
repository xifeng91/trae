# 旧版前端实现归档

本目录用于保存项目切换到 `Vite + Vue` 前的原生前端代码，方便后续对照旧逻辑或回溯交互实现。

目录内容：

```text
legacy-frontend/
├─ index.html
├─ css/
│  └─ style.css
└─ js/
   ├─ app.js
   └─ newsData.js
```

当前项目运行入口已迁移到 `news-web/index.html` 和 `news-web/src/`，这里的文件不再参与构建和线上访问。
