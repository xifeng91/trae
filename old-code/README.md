# Old Code Archive

本目录保存前后端分离前的历史代码，仅用于参考和回溯，不参与当前构建或部署。

```text
old-code/
├─ backend/                 # 旧 Express 单体服务使用的业务模块
├─ data/                    # 旧服务缓存数据
├─ reference/legacy-frontend/ # 旧原生 HTML/CSS/JS 前端
├─ server.js                # 旧 Express 单体入口
└─ .env.example             # 旧单体环境变量示例
```

当前前端入口在 `../news-web`，当前后端入口在 `../news-api`。
